# Physical constants — single source of truth for the entire project.
# Never define these values in any other file.

M = 0.5    # Wheel + base mass            (kg)
m = 0.2    # Body (pendulum) mass         (kg)
L = 0.1    # Wheel axis to body CoM       (m)
I = 0.006  # Body moment of inertia       (kg·m²)
b = 0.1    # Rolling friction coefficient (N·m·s)
g = 9.81   # Gravitational acceleration   (m/s²)
